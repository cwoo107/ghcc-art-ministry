<!DOCTYPE html>
<html lang="en">
<head>
  <title>GhccArtMinistry</title>
  <meta name="description" content="GHCC Art Ministry | Exodus: A Collection of Works">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= favicon_link_tag asset_path('favicon.png') %>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>

</head>

<body class="background-gradient flex flex-col min-h-screen">
<header class="absolute inset-x-0 top-0 z-50">
  <%= render "layouts/navbar" %>
</header>
<div class="flex-grow flex flex-col">
<main class="flex-grow relative" >
  <div class='bg-my-image bg-image '>
    <div class=" px-6 pt-14 lg:px-8 ">
      <div  class="mx-auto max-w-4xl py-48 pb-32" >
        <%= yield %>
      </div>
    </div>
  </div>
</main>
<footer class="" >
  <%= render "layouts/footer" %>
</footer>
</body>
<script>
    window.addEventListener('contextmenu', function(e) {
        if (e.target.nodeName === 'IMG') {
            e.preventDefault();
        }
    });

</script>
</html>
